@using Academy.Presentation.ViewModels;

@model  PageDataViewModel<CommentViewModel>

@{
    Layout = "PageView.cshtml";
}

@section PageData
{
    @foreach (var comment in Model.Items)
    {
        Html.RenderPartial("RenderTemplates/SingleCommentView", comment);
    }
}
