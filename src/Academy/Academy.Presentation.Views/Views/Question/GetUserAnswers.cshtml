@model Academy.Presentation.ViewModels.UserViewModel

@if (Model.Answers != null)
{
    foreach (var answer in Model.Answers)
    {
        Html.RenderPartial("RenderTemplates/SingleAnswerView", answer);
    }
}
