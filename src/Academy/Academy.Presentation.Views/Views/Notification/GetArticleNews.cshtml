@model Academy.Presentation.ViewModels.UserViewModel

@if (Model.ArticleNews != null)
{
    foreach (var viewModel in Model.ArticleNews)
    {
        Html.RenderPartial("RenderTemplates/ArticleNewsView", viewModel);
    }
}